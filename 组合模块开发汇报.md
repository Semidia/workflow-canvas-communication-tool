# 组合模块开发汇报

> 对象：画布工具本体（`画布工具本体/前端`）
> 汇报时间：2026-09-12
> 验证地址：测试脚本会自动探测本仓库这一份前端服务（本次跑在 `http://127.0.0.1:4174/index.html`），并校验服务上的 `app.js` 与本仓库文件字节一致后才开跑
> 截图产物：`D:\agent临时\郄的工作流画布沟通工具验收\`

---

## 一、一句话结论

**能。** 已经封装保存好的模块，随时可以重新打开来改，改完存回原模块；也可以只借它铺一份到画布上改，而完全不动原模块。这次做的就是这件事，已经用真实浏览器跑通并留了证据。

---

## 二、这次做的功能，一句话一份

「组合模块」= 把画布上挑出来的几个节点连同它们之间的连线，打包成一个有名字的**模块**存进模块库；以后在任何画布上，都能把这个模块整包**放**一份出来复用。

一颗模块从生到用，一共四步：

| 步骤 | 你怎么操作 | 背后发生了什么 |
|---|---|---|
| **封装** | 框选（或选中）几个节点 → 点「封装为模块」→ 起名字 | 把这些节点和「两端都在框选内」的连线存成模块，坐标归零（只存相对位置） |
| **放置** | 点「模块库」→ 找到模块 → 点「放置」 | 整包复制一份到当前画布中央，节点和连线都换成新编号，不会和原件打架 |
| **重命名 / 删除** | 模块库里点「改名」或「删除」 | 只影响模块库，不动作画布上的内容 |
| **打开来改** | 模块库里点「编辑」→ 改 → 点「保存回模块」 | 就地更新那一条模块，不新增条目（本次新增，见第三节） |

---

## 三、你问的那件事：封装好的模块还能再打开来编辑吗

### 3.1 结论

能。而且给的是**两条路**，你按心情挑：

- **想改原件** → 点「编辑」，改完点「保存回模块」，模块库里那一条就被更新了（不新增条目）。
- **只想借它改个变体，不动原件** → 点「放置」放一份到画布上随便改，或者点「编辑」改完点「取消编辑」。原模块一个字节都不动。

### 3.2 怎么用（大白话四步）

1. 顶部点「**模块库**」。
2. 在你要改的那条模块上，点「**编辑**」。
3. 模块的内容会像「放置」一样**铺到当前画布上**，并且**高亮**标出来——哪几个节点属于这个模块，一眼看得见。这时候你就能像平时一样改文字、拖动、加节点、拉线。
   - 画布顶部会出现一条**橙色悬浮条**，写着「正在编辑模块：<模块名>」，旁边两个按钮：`保存回模块`、`取消编辑`。看到这条，就说明在编辑态里。
   - **编辑中你新加的节点，也算这个模块的一部分**，会被一起存进去。
4. 改完：
   - 点「**保存回模块**」→ 模块就地更新，画布上你改的这些留下不动。
   - 点「**取消编辑**」→ 原模块不动，画布上你改的这些也留着（只是没写回模块）。
   - 按 `Esc` 也能退出编辑，会先问你一句再退。

### 3.3 为什么这么设计（动机 / 备选方案 / 代价）

**要解决的问题**：模块存下去以后，发现某个节点文字写错了、或者想给这个流程多加一步，如果只能「删掉重做」，那封装就成了负担。

**采用的方案：把模块「铺」到画布上改，改完写回。**

- **为什么不做一个单独的弹窗里的小编辑器？**
  因为画布上已有的全部能力（双击改字、拖动、缩放节点、连线、撤销、右侧信息栏）都要在弹窗里重做一遍。等于把整个编辑器写两遍，以后每加一个功能都要加两遍。**代价**：编辑时画布上会同时有你原来的内容和模块的内容，得靠高亮来分辨——所以我们把模块内容整块高亮出来了。

- **为什么「保存回模块」是就地更新、不新增一条？**
  因为你的心智是「我在改这个东西」，不是「我在做一个副本」。**代价**：改坏了没有「改回上一版」的后悔药——模块库本身不做版本历史。要留底请用「放置」先放一份，或定期用顶部「导出」另存 JSON。

- **为什么「取消编辑」不顺手把画布上铺出来的那份也删掉？**
  因为你可能在编辑过程中顺手改了画布上**别的**东西。如果取消时一并删掉，会连你不想丢的改动一起丢。所以取消只保证「原模块不变」，画布上的东西一律留着，你想删自己框选删掉即可。**代价**：取消后会留一份「多余的」节点在画布上，需要你手动收拾。

- **为什么编辑中要锁住一些操作？**（见下表）
  因为编辑态和「当前画布」是绑死的。如果编辑中切走画布，模块内容就散落在看不见的地方了，「保存回模块」会保存错东西。**代价**：编辑中你想开个新画布对个图，得先保存或取消——多按一下。

### 3.4 编辑态的锁定规则（字段级契约）

| 操作 | 编辑中是否允许 | 被拦下时的提示 |
|---|---|---|
| 再次点某模块的「编辑」 | ❌ 拦下 | `正在编辑模块「<名>」，请先「保存回模块」或「取消编辑」` |
| 删除**正在编辑的**那个模块 | ❌ 拦下 | `模块「<名>」正在编辑中，请先「保存回模块」或「取消编辑」` |
| 改这个模块的名字 | ✅ 允许（编辑条上的名字同步变） | — |
| 新建画布 | ❌ 拦下 | `模块「<名>」正在编辑中，请先「保存回模块」或「取消编辑」` |
| 关闭**正在编辑所在的**那个画布 | ❌ 拦下 | `模块「<名>」正在这个画布上编辑，请先「保存回模块」或「取消编辑」` |
| 切换/新建其他画布 | ❌ 拦下 | 同上 |
| 导入 JSON | ✅ 允许，但会**自动退出编辑** | 先退出编辑态再导入 |
| 「封装为模块」（另存一个新模块） | ✅ 允许，但会**自动退出编辑** | — |
| 保存时模块内容被删空 | ❌ 拦下 | `模块内容已被删空。若确实想清空，请直接删除该模块；或先「取消编辑」` |
| 刷新页面 | — | 编辑态**不跨刷新**：刷新后自动退出编辑，但**模块内容完好** |
| `Esc` / 关闭标签页 / 关闭浏览器 | ✅ | 有未保存的编辑时，`Esc` 会先确认 |

### 3.5 数据结构契约（可直接实现的字段级）

模块库存在浏览器本地，键名 `workflow-canvas-module-library-v1`，值是模块数组。

```yaml
Module:
  id:        string    # 必填，"module-<uuid>"，同库内唯一
  name:      string    # 必填，去首尾空格；空串回落为 "未命名模块"
  createdAt: number    # 封装时刻（毫秒时间戳）；老数据缺省时列表不显示时间
  nodes:     ModuleNode[]   # 必填且非空；空数组的模块在读取时会被丢弃
  edges:     ModuleEdge[]   # 可为空数组；只保留 from/to 都能在 nodes 里找到的

ModuleNode:
  id:     string   # 模块内部的相对编号，放置时会换成新的画布节点 id
  type:   enum     # rect | diamond | circle | document | triangle | text
  x:      number   # 相对坐标（存盘时把整块的 minX 减掉，所以最上/最左那个是 0）
  y:      number
  w:      number   # 限最小值（按形状各有最低安全尺寸）
  h:      number
  label:  string
  note:   string   # 可为空
  marker: enum     # 待讨论 | 已决定 | 有疑问 | 不采用

ModuleEdge:
  from: string   # 必须是本模块内某个 ModuleNode.id
  to:   string   # 同上；不允许 from == to，也不允许重复同向
```

**读取时的校验边界（`normalizeModule`）**：`id`/`name` 不是字符串 → 整条丢弃；`nodes` 不是数组或过滤后为空 → 整条丢弃；`edges` 里引用了不存在节点的连线 → 那条连线丢弃（不整条丢弃模块）。所以手改坏了的模块数据不会让整个模块库崩掉。

**编辑态的运行时状态**（只在内存里，不落盘）：

```yaml
moduleEditing:
  id:      string       # 被编辑的模块 id
  name:    string       # 冗余存一份，供编辑条显示（改名时同步）
  nodeIds: Set<string>  # 画布上「属于这个模块」的节点 id 集合
                        # 打开编辑时 = 铺出来的那批；编辑中新增的节点会加进来
```

**保存回模块的语义**：取「画布上 `nodeIds` 里的全部节点」，重新算 `minX/minY` 并整体减掉（相对坐标归零）写回 `m.nodes`；`m.edges` 取「两端都在 `nodeIds` 内的画布连线」。节点数归零 → 拒绝保存并提示。

---

## 四、验证证据

### 4.1 本次新增功能的专项验收

脚本：`前端/tests/module-inplace-edit-acceptance.mjs`
运行：`node tests/module-inplace-edit-acceptance.mjs`（不传参数时自动探测本仓库这一份前端服务，并校验 `app.js` 字节一致）
结果：**40 条断言全过，0 个页面报错**。截图：`D:\agent临时\郄的工作流画布沟通工具验收\模块就地编辑验收.png`

| 场景 | 覆盖内容 |
|---|---|
| 1 | 打开编辑 → 模块内容铺到画布（4+2=6 个节点、3+1=4 条连线、2 个高亮）→ 双击改文字 → 保存回模块：模块库仍 1 条不新增、内容已更新为新文字、节点数仍 2、**相对坐标已归零**、**画布内容没被吃掉（仍 6 个节点）**；再放置一份能带着改过的文字出来 |
| 2 | 取消编辑：原模块内容**原封不动**，画布上的改动保留 |
| 3 | 编辑中禁止重复打开 / 禁止删除该模块 / 禁止新建画布；保存后锁定解除，能正常新建画布 |
| 4 | 编辑中新增的节点会被算进模块（2 → 3 个节点），提示显示更新后的节点数 |
| 5 | 编辑中把节点删空后保存被拒绝，模块内容不被破坏，仍停留在编辑态 |
| 6 | 编辑中刷新页面：编辑条不残留（编辑态不跨刷新），模块内容完好 |

### 4.2 既有功能回归

全部 **8 个套件、共 8 次跑完、退出码全 0、0 个页面报错**（本次最后一遍是**不传任何参数**跑的，靠自动探测找到服务）：

| 测试脚本 | 结果 |
|---|---|
| `tests/workflow-canvas-acceptance.mjs` | ✅ 通过 |
| `tests/module-encapsulation-acceptance.mjs` | ✅ 通过 |
| `tests/module-fixes-acceptance.mjs` | ✅ 通过（10 个场景全过） |
| `tests/module-edit-lifecycle-acceptance.mjs` | ✅ 通过（10 项全过） |
| `tests/module-focus-events-acceptance.mjs` | ✅ 通过（10 项全过） |
| `tests/module-inplace-edit-acceptance.mjs` | ✅ 通过（40 条断言） |
| `tests/module-debug-flow-builder.mjs` | ✅ 通过 |
| `tests/canvas-interaction-audit-acceptance.mjs` | ✅ 通过（68 条断言，见第五节） |

**测试怎么认准「跑的是这一份代码」**（新增，配套下面的风险 R1）：

以前各套件的默认端口不一致，而且只认端口号认不出「是不是本仓库这份代码」。现在统一走 `tests/_served-target.mjs`：

1. 传了地址 → 只校验这一个；**取服务上的 `/app.js` 与本仓库 `前端/app.js` 比对 SHA-256**，不一致就直接中止并打印两边的字节数和路径，绝不静默换端口接着跑。
2. 没传地址 → 从 4173 起逐个探测到 4181，取第一个**代码一致**的服务。

已实测的反例：另起一个假服务（`app.js` 17 字节）于 4180，脚本按预期拒绝，提示里写明「该服务上的 app.js：17 字节 / 本仓库的 app.js：57932 字节」。

### 4.3 debug 流程图也已封装成模块

调试用的流程图已经用画布真实画出来并封装成模块，截图：`D:\agent临时\郄的工作流画布沟通工具验收\debug流程图封装模块.png`；可复现脚本：`tests/module-debug-flow-builder.mjs`。

---

## 五、多轮子智能体 debug 的发现与处理

用两轮并行子智能体，从不同角度审同一份产物：
- 第一轮分 4 个视角做功能审查（拖动/缩放/框选、连线/连接器、多画布生命周期、缩放平移视口），产出问题清单并修复；
- 第二轮换 4 个视角复审，累计报出 14 项疑似缺陷。

第二轮报出的 14 项，本次又派了 4 个子智能体**回到真实浏览器逐条复核**（不许只看代码猜），结论如下——

第二轮报出的 14 项（去重后），本次又派了 4 个子智能体**回到真实浏览器逐条复核**（不许只看代码猜），并把这 14 项**逐条固化成了可重复运行的真实浏览器用例**：`前端/tests/canvas-interaction-audit-acceptance.mjs`（18 个场景、**68 条断言全过、0 个页面报错**，截图 `交互审计14项验收.png`）。这比「子智能体口头回报」硬——以后每次改完画布代码，跑一遍就知道有没有踩回去。

**一句话结论：14 项全部复现、全部修掉、全部留了自动用例；没有一项是「说到但没证据」。**

| # | 视角 | 报出的问题 | 严重度 | 性质 | 修法 | 对应用例 |
|---|---|---|---|---|---|---|
| 1 | 拖动/框选 | 多选后拖动，只有一个节点动，其余当场取消高亮 | 中 | 用户会踩 | 拖动时按选区整组位移，`drag.items` 记一组 | 1-1、1-1b |
| 2 | 拖动/框选 | 节点拖到边界钉住后继续拖，坐标没变却压了一条撤销 → Ctrl+Z 第一次「没反应」 | 中 | 用户会踩 | `changed` 改按**坐标是否真变**判定，不看屏幕位移 | 1-2 |
| 3 | 拖动/框选 | 西/北控制点顶到边缘时节点反向「长大」 | 中低 | 用户会踩 | 顶边时只回收尺寸、不反弹 | 1-3 |
| 4 | 连线/连接器 | 从端口拖线未做指针捕获，拖到窗口外松手后虚线粘在光标上 | 中 | 用户会踩 | 起点即捕获指针，另加 `lostpointercapture` 兜底自愈 | 2-4、回归2 |
| 5 | 连线/连接器 | 反向重复连线（A→B 与 B→A）几何完全重合，画面上分不清 | 低 | 需刻意构造 | `addEdge` 双向重复拒绝 | 2-5 |
| 6 | 连线/连接器 | **点节点 A 当连接源 → 删掉 A → 再点 B：画面上什么都没有，但脏边已写进存档并会导出到 JSON** | **高** | 用户会踩 | `deleteSelection` 清连接态；`addEdge` 校验两端节点确实存在 | 2-1 |
| 7 | 连线/连接器 | 节点与连线的选中高亮互不重绘，界面显示与真实选中不一致 → Delete 可能删错对象 | 中 | 用户会踩 | 两者互斥时同步重绘 | 2-2 |
| 8 | 连线/连接器 | Esc 与点空白都不能取消连接态，取消后高亮还留着 → 再点节点会偷偷连出一条线 | 中 | 用户会踩 | Esc / 点空白统一走 `resetSelection()` | 2-3 |
| 9 | 连线/连接器 | 点空白、按 Esc 都无法取消选中 | 低 | 用户会踩 | 同 8 的 `resetSelection()` | 1-4 |
| 10 | 多画布 | 画布 A 里框选 2 个节点后新建画布 B：B 是空的，底下却还写着「已框选 2 个节点」 | 中 | 用户会踩 | `createCanvas` 改调 `resetSelection()` | 3-1 |
| 11 | 多画布 | 关掉标签后不回到相邻标签，且新激活画布残留上一个的选中 | 中 | 用户会踩 | 关闭时回退相邻标签 + `resetSelection()` | 3-2 |
| 12 | 缩放平移 | **缩到 30% 后在画布外的灰色空白处点一下放节点：节点落到画布外（x≈2808 > 2400）；而视图又被锁在画布内，怎么拖都找不回来** | 中 | 用户会踩 | `addNode` 补右下边界夹紧，四边都对 | 4-4 |
| 13 | 缩放平移 | 点「100%」、读入已存视图、改窗口大小都不过 `clampPan` → 宽屏上画布贴左上、右侧空一大块，拖一下又跳回居中 | 低 | 需宽屏/4K 或改窗口 | `renderTransform()` 统一过 `clampPan()` | 4-1、4-2 |
| 14 | 缩放平移 | 滚轮没处理 `deltaMode`，Firefox 按「行」上报时每格只滚 3 像素，几乎推不动 | 低 | 需 Firefox | 按「行」=16 像素、按「页」=一屏换算 | 4-3 |

**三条修复主线**（14 项基本都归到这三条上）：

1. **选中状态只能有一个「清干净」的入口** —— 新增 `resetSelection()`，把 `selectedNodeId` / `selectedEdgeId` / `selectedNodeIds` / `resizeModeNodeId` / `connectorSourceId` / 进行中的连接和虚线一把清掉；新建画布、关画布、Esc、点空白全部走它。第 6、8、9、10、11 项都是「某处漏清了其中一两个」造成的。
2. **判断「有没有真的变」，而不是「有没有动过鼠标」** —— 撤销、拖动、缩放都改成比对坐标/尺寸的真实变化。第 2、3、12 项都源于「只看屏幕位移」。
3. **凡是能被系统强行打断的操作，都要有兜底** —— `lostpointercapture` 统一收尾，覆盖拖节点、拖控制点、拖线、框选、平移。第 4 项和「回归2」都靠它。

**收敛判定**：这 14 项里 10 项属「用户会踩的实际缺陷」，4 项属「需刻意构造/特定环境」（第 5、13、14 项及第 3 项的极端贴边）。修完后再复核，**0 项复现、0 页面报错**；此后没有再出现新的「用户会踩的实际缺陷」，故**审计这一环收口**。
>
> ⚠️ 注意用词：收口的是「审计这一环」，**不是整个任务**。整个任务因为下面 R1 的启动器那条仍未关闭，所以**不算归档**，如实标注为「有未关闭门」。

**这一轮踩到的坑（已记进经验库）**：测试一开始报的 `workflow-canvas-acceptance` 失败（「expected 4 initial nodes, got 7」）**不是本次改动引起的回归**，而是端口 4173 上跑着**另一份旧代码**。这也正是下面风险 R1 的由来。

---

## 六、未关闭门与风险表

| 编号 | 事项 | 类型 | 现状 | 触发条件 | 下一步 |
|---|---|---|---|---|---|
| R1 | **这台机器上可能有「另一份旧画布」跟你抢 4173 端口** | 环境 | 已核实（当时）：`D:\郄的工作流画布沟通工具\画布工具本体\后端\canvas_server.py` 占着 4173，服务的是**另一份旧代码**（`app.js` 94,830 字节、没有模块库）。本次复核时该服务已停，4173 当前空闲 | 你在旧副本的服务还开着时双击 `启动画布工具.bat`：脚本会问「1 终止重启 / 2 打开正在运行的」。**选 2 会打开旧副本**，看不到模块功能 | **测试侧已根治**：8 个套件统一走 `tests/_served-target.mjs`，靠 `/app.js` 的 SHA-256 与本仓库文件比对来认人，认错就中止并打印两边字节数，不再靠端口号猜（反例已实测）。**启动器侧仍待你点头**：建议改 `启动画布工具.ps1` 的 `Test-CanvasPage`，把现在「页面里有『设计沟通画布』和 `app.js` 就算数」换成「比对候选端口 `/app.js` 与本地 `前端/app.js` 是否一致」——**此项需你同意后再改** |
| R2 | 模块库没有版本历史 | 设计代价 | 已接受 | 改错模块后想回到上一版 | 用「放置」留副本，或定期「导出」JSON |
| R3 | 「取消编辑」后画布上会留一份铺出来的节点 | 设计代价 | 已接受 | 取消编辑后 | 想清掉就框选删掉 |
| R4 | 编辑态期间无法新建/切换画布 | 设计代价 | 已接受 | 编辑中想开新画布 | 先保存或取消 |
| R5 | 功能测试需要本地 HTTP 服务在跑 | 环境 | 已缓解 | 未启动服务就跑测试 | 脚本会自动从 4173 起探测本仓库这一份服务；一个都没找到时，报错里会写明「本仓库的 app.js 应为 57932 字节」并给出启动命令 |

> R1 是本次复核时**意外发现**的：跑 `workflow-canvas-acceptance.mjs` 报「expected 4 initial nodes, got 7」，追下去才发现 4173 上是另一份旧代码在服务，不是本次改动引起的回归。**这不是本次工作引入的问题，但会直接影响你「双击启动」时看到的是不是新版本**，所以登记在此。
>
> 现状：**测试这一头已经根治**（改用「比对 app.js 内容」认人，见 4.2）；**启动器那一头还没动**，因为改 `启动画布工具.ps1` 属于你日常双击的入口，我不想不打招呼就改。**这是一个仍未关闭的门**：在你点头之前，如果旧副本的服务开着而你双击启动又选了「2」，看到的仍是旧版本。
>
> 另外这一轮还踩到一个本机坑并已沉淀：Node 里从 `import.meta.url` 取中文路径时，直接读 `url.pathname` 会拿到 `%E9%83%84` 这样的百分号编码，必须用 `fileURLToPath()` 解码，否则 `scandir` 直接报「找不到目录」。

---

## 七、那么，代价是什么？

**最大的代价是「编辑」这个动作把模块和画布临时缝在了一起。** 打开编辑的那一刻，模块的内容就真的变成画布上的节点了——它不再是「一份存档」，而是一份**活着的、能被你误删误改的**内容。为了不让这份临时状态出错，我们额外加了一整套锁：编辑中不能切画布、不能新建画布、不能删这个模块、删空了不让保存。这套锁本身也是复杂度，也是以后每次动「画布生命周期」相关代码时都要记得照顾的地方。

**第二个代价：改模块没有后悔药。** 就地更新意味着没有「上一版」。这跟「画布本身有撤销栈」不一致——画布上你按 `Ctrl+Z` 能退，模块库退了没有。这是刻意的取舍（模块库保持简单），但确实是一个需要你知道的边界。

---

## 八、本轮对我自己的质疑（以及回应）

**质疑**：这次的「打开来改」是**叠加到画布上**做的，不是独立编辑器。那如果用户本来就是想「独立、干净地只改这个模块」，画布上原有的 4 个节点会不会反而碍事、甚至让他误改？

**回应**：会，所以做了三层防护——模块内容整块高亮、画布背景变淡橙、顶部常驻橙色编辑条。但从实测（场景 1）看，画布上确实同时存在 6 个节点，用户双击改字时仍需要靠高亮分辨「哪个是模块的」。**这是真实的可用性未关闭门**：如果后续你反馈「看不过来」，正确的下一步是做**独立的模块编辑视图**（临时把画布切成只显示这一个模块的单画布），而不是继续在叠加态上加提示。

**质疑二**：这次为了「不再认错版本」新加的 `_served-target.mjs`，是不是**为了防一个偶发问题而给每个测试都加了一道网络请求**？万一哪天服务慢了、或换了端口范围，会不会反而让测试更容易跑不起来？

**回应**：这个担心是实的，所以做了两个约束：① 探测超时只有 1.5 秒，且只在 4173–4181 这 9 个端口上试，一个都不通才报错——报错信息里直接写明「本仓库 app.js 应为 57932 字节」和启动命令，比原来「连接被拒绝」这种干巴巴的报错好排查；② 你随时可以手动传地址（`node tests/xxx.mjs http://127.0.0.1:端口/index.html`）跳过探测，但**这时仍然会校验内容**——这是刻意的：跳过「找」，不跳过「认」，因为认错版本正是这次踩的坑。**代价**是：如果哪天你把 `app.js` 改名或拆成多个文件，这个校验要跟着改，否则所有测试会一起报「不是这一份」。这是已知的耦合点，登记在此。
