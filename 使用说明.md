# 设计沟通画布 · 使用说明（给其他人）

> 本工具是**独立产品**「郄的工作流画布沟通工具」，**不是**民诉法云端课堂里的正式时间线画布。  
> 两套画布不要混：民诉画布管课堂提交/连线；本工具只做人与 AI 的设计讨论。

---

## 1. 它是干什么的

把产品逻辑**画清楚再讨论**：放节点、写说明、拉线、打讨论标记，导出 JSON 或截图交给 AI 复盘；AI 也可以直接改磁盘上的画布状态文件再「从磁盘加载」。

**不是**：智能体执行器、业务系统、民诉课堂画布、会自动上云的协作白板。

---

## 2. 怎么打开

**唯一入口**（本目录）：

```text
双击  启动画布工具.bat
```

可选换端口：

```text
启动画布工具.bat -Port 4186
```

启动后浏览器打开：

```text
http://127.0.0.1:4173/index.html
```

（若 4173 被占，启动台会尝试下一端口。）  
依赖：PowerShell 7、Python。关掉启动台窗口会结束本次本地服务。

**不要**直接双击 `前端\index.html`，也不要用浏览器 `file://` 打开——那不是正式入口：磁盘保存、`/api/state`、部分右键菜单会失效。改代码后看不到新界面时，仍走启动台，并强刷 `Ctrl+F5`。

---

## 3. 日常操作（五分钟上手）

| 操作 | 怎么做 |
|------|--------|
| 放节点 | 左侧选「步骤 / 判断 / 开始结束 / 材料 / 提醒」→ 点空白画布 |
| 改文字 | **双击**节点文字 |
| 连线 | 左侧「连线」先点 A 再点 B；或**拖节点边缘小圆点**——拖拽中**所有卡片四边/中点锚点会全部亮起**，拖到目标锚点松手即连，松手后隐藏 |
| 讨论标记 | 选中节点 → 右侧「节点信息」→ 待讨论 / 已决定 / 有疑问 / 不采用 |
| 判断条件 | 选中菱形 → 右侧填「判断条件」「退出循环条件」 |
| **循环怎么画** | 连线勾「循环回边」（虚线）= 回到某步再走；**何时结束**必须在目标/相关**菱形**填「退出循环条件」 |
| 缩放节点 | 选中 → 右上角缩放图标 → 拖控制点 |
| 删 | 选中后 `Delete` |
| 平移画布 | 空白处拖动 |
| 画布缩放 | 底部 `− / % / ＋`（1%–220%） |
| 保存浏览器 | 顶栏「保存」（改动约 600ms 也会自动存草稿） |
| 导出/导入 JSON | 顶栏「导出」「导入」 |
| **给 AI 看** | 右键节点/连线/画布标签 → 复制文本快照或 JSON |
| **复制画布 ID** | 右键**画布标签** →「复制画布 ID」/「复制画布 ID + 名称」 |
| **写到磁盘** | 右键 →「保存到磁盘」→ `工作流导出/画布数据.json` |
| **从磁盘读** | 右键 →「从磁盘加载」（覆盖当前画布） |
| **磁盘变更提示** | 状态栏「真相」：浏览器草稿 / 磁盘已同步 / **磁盘已更新**；干净页约 0.8s 自动重载，有草稿时点「磁盘已更新，点击重载」 |

模块：框选若干节点 →「封装为模块」→ 模块库可放置 / 编辑回写 / 删除。  
**落盘**：每次改模块库会先写浏览器 localStorage，再自动镜像到 `工作流导出/模块库.json`（独立文件，不并入画布数据.json）；清浏览器数据后刷新，若本地为空会从该磁盘稿恢复。「导出模块库」仍是浏览器下载，便于换机拷贝。详见 `docs/组合模块开发汇报.md`。

---

## 3.1 循环怎么画、怎么结束

| 概念 | 填在哪 | 作用 |
|------|--------|------|
| **判断条件** `condition` | 菱形「判断条件」 | 这一步在问什么（例如「材料是否齐全？」） |
| **退出循环条件** `exitCondition` | 菱形「退出循环条件」 | **何时不再绕回去**（例如「齐全，或已核对 3 轮」） |
| **循环回边** `loop` | 连线勾选「循环回边」 | 只表示「画了一条回去的虚线」，**不**等于结束标准 |

画法建议：

1. 主路径：步骤 → 菱形判断 →（是）下一步。
2. 回边：从后续步骤拉回菱形（或回前面的步骤），选中连线勾「循环回边」。
3. **结束标准**：选中**目标菱形**，在右侧填「退出循环条件」。  
   - 勾了 loop 但菱形未填 → 检查器会红字提示「循环结束条件未填写…」，状态栏也会提示。  
   - 右键文本快照里该边带 `结束条件=…`；空则 `结束条件=未填写`。

`condition` 管「怎么分叉」，`exitCondition` 管「何时结束循环」——两者都要填。

---

## 4. 和 AI 怎么配合

1. **人画图** → 右键「复制画布文本快照」贴给 AI；或 AI 直接读  
   `工作流导出/画布数据.json`。
2. **AI 改图** → 编辑该 JSON（保持 `{version, activeCanvasId, canvases}` 结构）→ 页面右键「从磁盘加载」；或  
   用 HTTP（路径 ASCII，见 **[docs/AI对接.md](docs/AI对接.md)**）：  
   - 整包：`GET/POST http://127.0.0.1:4173/api/state`  
   - **单画布**：`POST /api/state?canvasId=…`  
   - **单节点字段**：`PATCH /api/state?canvasId=…&nodeId=…`（body 如 `{"label":"..."}`）
3. 也可「从剪贴板粘贴 JSON 回写」（单画布 `{id,name,nodes,edges}` 或整体 `{canvases:[...]}`）。
4. 磁盘被外部改过时，状态栏「真相」显示**磁盘已更新**并出现「磁盘已更新，点击重载」——本页无未保存改动时会**约 0.8 秒自动重载**；有草稿则等你一键确认，**不会**静默覆盖。

节点类型：`rect` 步骤 · `diamond` 判断 · `circle` 开始结束 · `document` 材料 · `triangle` 提醒 · `text` 文字。  
字段含义见页面右键文本快照自带的说明头。

---

## 5. 数据与隐私

| 存储 | 位置 |
|------|------|
| 浏览器草稿 | 本机 localStorage（不自动上云） |
| 磁盘权威稿（画布） | `工作流导出/画布数据.json` |
| 磁盘权威稿（模块库） | `工作流导出/模块库.json`（`saveModules` 自动 POST `/api/modules` 镜像；本地非空时启动**本地优先**） |
| 导出文件 | 你选的路径；公开仓库已忽略 `工作流导出` 与 `*.workflow.json` |

无账号、无云端同步、无模型 API。是否把 JSON 交给某个 AI，由你每次决定。

**导出到项目文件夹时**：民诉等业务材料放对应「××项目用」分区；不要把密钥、课堂真实数据、运行库丢进本工具目录。

---

## 6. 目录结构（本体）

```text
画布工具本体/
├─ 启动画布工具.bat     # 唯一双击入口（纯 ASCII）
├─ launch.ps1           # 引导：拼中文路径、转发 -Port
├─ readme.md            # 总导航（本文件的上一级说明）
├─ 使用说明.md          # ← 你正在读的这份（给使用者）
├─ docs/                # 说明与历史汇报（不参与运行）
│   ├─ 架构说明.md
│   ├─ AI对接.md        # /api/state·/api/modules curl·node 示例（ASCII 路径）
│   └─ 组合模块开发汇报.md
├─ 前端/                # 页面与交互（index.html + 分模块 js + tests）
├─ 后端/                # 启动 ps1 + canvas_server.py（静态 + /api/state + /api/modules）
└─ 工作流导出/          # 画布数据.json、模块库.json 等落盘
```

更完整的架构见 `docs/架构说明.md`。

---

## 7. 测试

在 `前端` 目录：

```text
node tests/workflow-canvas-acceptance.mjs http://127.0.0.1:4173/index.html
```

---

## 8. 常见问题

| 现象 | 处理 |
|------|------|
| 提示找不到 PowerShell 7 | 安装 PowerShell 7 后再点启动台 |
| 端口占用 | 换 `-Port 4186`，或按提示终止旧画布 / 打开现有画布 |
| 改了代码页面还是旧的 | 服务已带 `Cache-Control: no-store`；强刷 `Ctrl+F5` |
| 「从磁盘加载」没反应 | 先「保存到磁盘」；或确认 `工作流导出/画布数据.json` 存在 |
| 状态栏显示「磁盘已更新」 | 磁盘被改过；干净页会自动重载，或点「磁盘已更新，点击重载」；也可显式「保存到磁盘」覆盖 |
| 右键没出现「复制画布 ID」 | 确认右键的是**画布标签**；强刷 `Ctrl+F5` |
| 拖连线时看不到别的节点锚点 | 强刷 `Ctrl+F5`；拖拽中应**所有**节点四边/中点锚点可见，松手/取消后隐藏 |
| 控制台报「脚本加载顺序自检失败」 | 某个前端 js 没加载上；强刷或检查启动台端口是否指向本仓库 |
| 和民诉时间线搞混 | 顶栏橙色角标常显「≠ 课堂/正式时间线画布」；民诉课堂画布在云端课堂产品里，本工具只谈设计 |
| 直接开了 `前端\index.html` | 不是正式入口；关掉后**只走** `启动画布工具.bat` |

---

## 9. 版本

**v1.2.0**（2026-09-24）：
- 细粒度写 API + 磁盘已更新/自动重载（`4bf451c`）。
- 模块库磁盘权威 `/api/modules` 落盘（`7cac3fd`）。
- 连线拖拽全锚点（`is-linking`）+ 文档口/身份角标 FAQ（332 本版含，待并）。
- 循环结束条件醒目化、右键复制画布 ID 等（v1.1.0 已有，`6ad8fd2`）。
- `test_api_state` / `test_api_modules` 自测全过。

**v1.1.2**（2026-09-24）：模块库磁盘权威——`/api/modules` 读写 `工作流导出/模块库.json`，`saveModules` 静默镜像，启动本地空时可从磁盘恢复。

**v1.1.1**（2026-09-24）：细粒度写 API（按 canvasId / nodeId / edgeId 的 POST·PATCH）、磁盘变更「磁盘已更新」状态与干净页自动重载。

**v1.1.0**（2026-09-24）：磁盘轮询与真相状态条、右键复制画布 ID、循环结束条件醒目化、API 示例与脚本加载自检。

### 9.1 UI/文档路（已并入 v1.2.0）

- **根文档统一**：`readme.md` / `使用说明.md` / `前端/readme.md` / `docs/架构说明.md` / `后端/readme.md` 写死唯一入口 `启动画布工具.bat`；明确禁止直接开 `前端\index.html`（`file://` 下磁盘保存 / `/api/state` / 部分右键会失效）。
- **UI 身份角标**：顶栏品牌区常显「≠ 课堂/正式时间线画布」，与标题「设计沟通画布」连读为完整产品边界。
- **拖动连线显示全部锚点**：`connecting` / `reattaching` 期间视口挂 `is-linking`，所有节点四边/中点端口可见、可拖到即连；结束（松手 / Esc / 取消捕获 / 切工具）卸掉类名并隐藏。
- **复查通过**：右键画布标签「复制画布 ID」「复制画布 ID + 名称」仍在首项；菱形 `exitCondition` 字段与循环回边未填结束条件的红字警告仍在。

---

## 10. 独立仓库

源码历史独立于民诉产品：`https://github.com/Semidia/workflow-canvas-communication-tool`  
上级目录按项目分区：`民诉项目用` / `交易台项目用` / `轮询器项目用` / `其他`（只放导出与截图，不放运行数据）。
